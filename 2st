<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8">
<title>AI 음성 회의록</title>

<style>

body{
font-family: Arial;
background:#f2f4f7;
padding:40px;
}

.container{
max-width:800px;
margin:auto;
background:white;
padding:30px;
border-radius:10px;
box-shadow:0 10px 20px rgba(0,0,0,0.1);
}

h1{
text-align:center;
}

button{
padding:12px 20px;
font-size:16px;
margin:10px 5px;
border:none;
border-radius:5px;
cursor:pointer;
}

.start{
background:#28a745;
color:white;
}

.stop{
background:#dc3545;
color:white;
}

.summary{
background:#007bff;
color:white;
}

textarea{
width:100%;
height:150px;
margin-top:15px;
padding:10px;
font-size:14px;
}

.result{
margin-top:20px;
background:#f1f3f5;
padding:20px;
border-radius:8px;
}

.timer{
text-align:center;
margin-top:10px;
color:#888;
}

</style>
</head>

<body>

<div class="container">

<h1>🤖 AI 음성 회의록</h1>

<p>마이크로 회의 내용을 말하면 자동으로 기록됩니다.</p>

<button class="start" onclick="startRecording()">🎤 녹음 시작</button>
<button class="stop" onclick="stopRecording()">⏹ 녹음 중지</button>
<button class="summary" onclick="makeSummary()">📌 회의 요약</button>

<textarea id="transcript" placeholder="음성 인식 결과가 여기에 표시됩니다..."></textarea>

<div class="result" id="summaryResult">
요약 결과가 여기에 표시됩니다.
</div>

<div class="timer">
이 웹페이지 제작 시간 ⏱ 15분 (AI와 함께 제작)
</div>

</div>

<script>

let recognition;

if ('webkitSpeechRecognition' in window) {

recognition = new webkitSpeechRecognition();
recognition.lang = "ko-KR";
recognition.continuous = true;

recognition.onresult = function(event){

let transcriptBox = document.getElementById("transcript");

for(let i = event.resultIndex; i < event.results.length; i++){
transcriptBox.value += event.results[i][0].transcript + " ";
}

};

}

function startRecording(){

recognition.start();

}

function stopRecording(){

recognition.stop();

}

function makeSummary(){

let text = document.getElementById("transcript").value;

let sentences = text.split(/[.!?]\s/);

let summary = sentences.slice(0,3);

let result = "<h3>📌 핵심 요약</h3><ul>";

summary.forEach(s=>{
result += "<li>"+s+"</li>";
});

result += "</ul>";

document.getElementById("summaryResult").innerHTML = result;

}

</script>

</body>
</html>
